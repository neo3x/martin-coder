"use client";
import { PageShell } from "@/components/marketing/page-shell";
export default function ProductPage(){const es=typeof document!=="undefined"&&document.cookie.includes("locale=es");return <PageShell><h1 className="text-3xl font-semibold mb-4">{es?"Producto":"Product"}</h1><p className="text-muted-foreground max-w-3xl">{es?"MartinCoder combina arquitectura robusta, interfaz premium y seguridad operativa para equipos que trabajan sobre repositorios reales.":"MartinCoder combines robust architecture, premium UX, and operational safety for teams working in real repositories."}</p><div className="mt-8 grid md:grid-cols-3 gap-4"><div className="card p-4"><h3 className="font-medium mb-2">Trust</h3><p className="text-sm text-muted-foreground">Approvals, safety boundaries, and transparent tool traces.</p></div><div className="card p-4"><h3 className="font-medium mb-2">Control</h3><p className="text-sm text-muted-foreground">Project, session, provider, and model management in one workspace.</p></div><div className="card p-4"><h3 className="font-medium mb-2">Velocity</h3><p className="text-sm text-muted-foreground">From plan to build without losing context.</p></div></div></PageShell>}
